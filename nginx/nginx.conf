server {
  listen 80;
  # Use '_' as a catch-all. Replace with your actual domain name (e.g., example.com) if needed.
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri /index.html;
  }
}
